new Text:BACKROUND_HBE[index];
new PlayerText:HBE_LAPAR[MAX_PLAYERS];
new PlayerText:HBE_HAUS[MAX_PLAYERS];
new PlayerText:HBE_NYAWA[MAX_PLAYERS];

stock LoadPlayerTextdraw(playerid) {
  // speedo value
  
  
  // hbe
  
  
  return 1;
}

stock LoadTextdraw() {
  // speedo backround
 
  // hbe
 
  return 1;
}
